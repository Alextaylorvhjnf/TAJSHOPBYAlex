# TAJ Electronics — v29 (Release Notes)

نسخهٔ ۲۹ بر پایهٔ v28 بدون تغییر رفتار موجود ساخته شده است — هیچ خطی از منطق v28
عمداً تغییر نکرده؛ فقط قابلیت‌های جدید اضافه و باگ‌های گزارش‌شده رفع شده‌اند.

## ۰) کاتالوگ نمونه کامل — همراه نسخه (جدید)
- کل داده‌های نمونهٔ v29 همان داده‌های v28 است و این بار در «هر» نوع نصب به‌صورت
  خودکار اعمال می‌شود:
  ۳۷ محصول (۹۳ تصویر گالری) · ۲۱ دسته‌بندی · ۱۶ برند · ۳ اسلایدر · ۱۶ استوری ·
  ۲ شوکیس · ۸ صفحهٔ راهنما · ۱۲ لینک فوتر · ۲ کد تخفیف · ۸ روش ارسال +
  تنظیمات نمونهٔ فروشگاه/پرداخت (اعلان، تیکر، ساعات کاری، کارت به کار و…).
- ویزارد نصب حالا یک گزینهٔ «نصب کاتالوگ نمونه فروشگاه» دارد (پیش‌فرض روشن):
  بعد از ساخت حساب مدیر، کاتالوگ نمونه به‌صورت خودکار وارد دیتابیس می‌شود.
  اگر فروشگاه خالی می‌خواهید کافی است تیک را بردارید.
- ایمن و تکرارپذیر: اگر دیتابیس از قبل محصول داشته باشد (مثلاً seed اولین بوت
  Docker) هیچ چیزی بازنویسی نمی‌شود و این مرحله رد می‌شود.
- فایل db/catalog-seed.db (همان seed همیشگی) همراه نسخه است و جریان Docker هم
  مثل v28 کار می‌کند؛ دیتابیس واقعی شما هرگز بازنویسی نمی‌شود.

## ۱) صفحهٔ تعمیر (Repair Page) — قالب‌های جدید + انتخاب از پنل
- ۴ قالب آماده: tech-dark (نمای v28)، minimal-light، neon-glass و countdown-eta.
- انتخاب قالب از: پنل ← تنظیمات ← فروشگاه ← حالت تعمیر با پیش‌نمایش تصویری.
- هر واژهٔ صفحهٔ تعمیر (عنوان، توضیح، نشان، برچسب‌ها، بخش و دکمهٔ پیگیری، یادداشت
  پایین) از همان‌جا قابل ویرایش است — خالی = واژهٔ پیش‌فرض طراحی.

## ۲) دکمهٔ «ورود مدیران» حذف و «پیگیری سفارش» جایگزین شد
- دکمهٔ ورود مدیران روی صفحهٔ تعمیر کاملاً حذف شد و به‌جای آن دکمهٔ «پیگیری سفارش»
  (متنی قابل ویرایش) قرار گرفت.
- صفحهٔ /track-order حتی وقتی فروشگاه در حالت تعمیر است باز می‌ماند تا مشتری‌ها
  سفارش خود را ردیابی کنند. مدیران مثل قبل مستقیم از /admin وارد می‌شوند.

## ۳) رمز فراموش‌شده + کد بازیابی (Recovery Phrase)
- بعد از اولین نصب، در صفحهٔ پایانی ویزارد نصب یک کد بازیابی (عبارت تصادفی) تولید
  و فقط یک بار نمایش داده می‌شود + هشدار «در جای امن ذخیره کنید».
- در صفحهٔ ورود مدیران دکمهٔ «رمز عبور را فراموش کرده‌اید؟ ورود با کد بازیابی»
  اضافه شد — با شناسه + کد بازیابی وارد شوید و از «حساب من» رمز جدید بگذارید.
- از پنل ← حساب من ← کد بازیابی حساب می‌توانید هر زمان کد جدید بسازید (کد قبلی
  باطل می‌شود). کد فقط هش SHA-256 ذخیره می‌شود و هرگز دوباره قابل نمایش نیست.

## ۴) رفع باگ‌ها
- فاوآیکون: آپلود فاوآیکون حالا واقعاً در تب مرورگر اعمال می‌شود.
- لوگوی فوتر: حالا در فوتر مشترک و فوتر همهٔ قالب‌ها رندر می‌شود.
- رفرش در تب برندینگ: تب فعال در URL می‌ماند (?tab=branding).
- آواتار ناهماهنگ: هدر و منوی کناری بعد از تغییر آواتار هم‌زمان به‌روز می‌شوند.
- کاتالوگ نمونه: داده‌های نمونهٔ v28 (محصولات، اسلایدرها، برندها، دسته‌بندی‌ها و
  همهٔ محتوا) حالا در نصب تازه هم به‌صورت خودکار اعمال می‌شود (بند ۰).

## ۵) آواتار مدیر (مثل پروفایل کاربران)
- پنل ← حساب من ← آواتار مدیر: ۸ چهرهٔ آماده + لوگوی فروشگاه + آپلود تصویر.
- نمایش در منوی کناری پنل، کارت «حساب من» و هدر سایت.

## ۶) لوگوی دستیار هوشمند (ویجت چت AI)
- تنظیمات ← هوش مصنوعی ← لوگوی دستیار: آپلود هر تصویر + انتخاب قالب‌های اعمال +
  پیش‌نمایش زندهٔ دکمهٔ شناور.

## ۷) راهنماهای پنل مدیریت
- راهنمای کوتاه برای گزینه‌های اصلی تنظیمات + راهنمای سه‌لایه‌ای فوتر + راهنمای
  حالت خالی لینک‌های هر قالب.

## سازگاری / نصب
- ستون‌های جدید: User.recoveryCodeHash و StoreSettings.maintenanceTemplate /
  maintenanceContent / aiWidgetLogo / templateAiLogos (db push کافی است).
- db/catalog-seed.db به اسکیمای v29 مهاجرت داده شده (۳۷ محصول، دست‌نخورده).
- API جدید (فقط افزوده): POST /api/install/demo-catalog — فقط در زمان ویزارد نصب
  فعال است و غیرمخرب است.
- رفتار همهٔ مسیرها و APIهای موجود v28 بدون تغییر است.

---

# v29.1 — رفع مشکلات نصب روی سرور (Deploy-over-Volume)

مشکل گزارش‌شده روی سرور واقعی: بعد از آپدیت v29 روی همان Docker volume قبلی،
صفحهٔ /install با ریدایرکت 307 به فروشگاه برمی‌گشت (ویزارد هرگز باز نمی‌شد) و
صفحات پنل/فروشگاه خطای 500 («خطا در ارتباط با سرور») می‌دادند. ریشهٔ هر دو
مشکل یکی بود: دیتابیس قدیمی (اسکیمای v28) + اجرای نشدن/شکست `prisma db push`
در کانتینر. سه رفع زیر اضافه شد:

## ۸) خودترمیمی اسکیما در زمان اجرا (Self-Heal)
- اولین بار که برنامه به دیتابیس دست می‌زند، ستون‌های نسخهٔ جدید که وجود
  ندارند با ALTER TABLE ساده اضافه می‌شوند (دقیقاً همان کاری که db push
  می‌کند) — صفحهٔ فروشگاه، ورود مدیران و همهٔ APIها حتی بدون db push سالم
  می‌مانند. غیرمخرب، تکرارپذیر و برای نسخه‌های بعدی قابل گسترش.
- نتیجه: آپگرید v28→v29 روی volume قدیمی هرگز دیگه 500 نمی‌دهد.

## ۹) اجرای مجدد ویزارد نصب (باز کردن قفل نصب)
- چرا لازم شد: نصبِ قدیمی، /install را برای همیشه قفل می‌کند تا ویزارد
  دوباره اجرا نشود — ولی بعد از آپدیت نسخه لازم است دوباره اجرا شود.
- دو راه (هر دو فقط پرچم نصب را به «نصب‌نشده» برمی‌گردانند؛ داده‌ها،
  محصولات، سفارش‌ها و کاربران دست‌نخورده می‌مانند):
  1. از پنل: تنظیمات ← تب فروشگاه ← کارت «ویزارد نصب اولیه» ←
     «اجرای مجدد ویزارد نصب» (فقط مدیر کل) → مستقیم به /install می‌رود.
  2. از ترمینال سرور: `docker compose exec app node scripts/reset-install.mjs`
- ویزارد ایمن و تکرارپذیر است: مدیر تازه (با کد بازیابی جدید) می‌سازد،
  تنظیمات خالی را پیش‌فرض می‌کند، کاتالوگ نمونه فقط وقتی محصولی وجود
  نداشته باشد نصب می‌کند و مدیران قبلی به کارشان ادامه می‌دهند.
- نکتهٔ فنی: پرچم «حذف» نمی‌شود بلکه «installed=false» می‌شود تا مسیر
  legacy-detection (وجود مدیر قدیمی = نصب‌شده) دوباره قفل نکند.

## ۱۰) دستیار هوشمند: لینک محصول + پاسخ به ادامهٔ گفتگو
- پاسخ‌های متنی دستیار حالا اسم محصولات را به لینک واقعی صفحهٔ محصول
  تبدیل می‌کنند: [نام محصول](/products/slug) — یک کلیک و مشتری در صفحهٔ
  محصول است (هم در پاسخ‌های LLM، هم پاسخ‌های فوری/بدون LLM).
- پیام‌های ارجاعی مثل «قیمتشونو ندادی» دیگر جستجوی لفظی نمی‌شوند (باگِ
  «محصولی مطابق پیدا نشد»)؛ به موتور گفتگو با تاریخچهٔ چت می‌روند و بر
  اساس همان محصولات قبلی، جواب گفتگویی می‌دهند.

---

# v29.2 — پنج گزارش کاربر: رفع باگ‌ها + قابلیت‌های جدید

## ۱۱) رفع نمایش‌داده‌نشدن تیترها در حالت روشن (قالب پاییز/زمستان/لوکس/نئون‌نوآر)
- ریشه: در نسخهٔ «پوست روشن» این قالب‌ها، گرادیان تیترها با شورت‌کات `background:`
  بازنویسی می‌شد که `background-clip: text` را به حالت اولیه برمی‌گرداند — متنِ
  شفاف زیر یک نوار رنگی می‌ماند و تیترهایی مثل «برگ‌های تازه»، «پرسش‌های دنج»،
  «برداشت پاییزی» و «کارت‌های پستال» دیده نمی‌شدند.
- رفع: همهٔ overrideها به `background-image:` تبدیل شدند (کلیپ متن حفظ می‌شود).
  قالب گیمینگ هم همان الگوی خطرناک داشت که در بازطراحی رفع شد.

## ۱۲) رفع ناپایداری SMTP («رمز ذخیره نمی‌شود»)
- ریشه: کش تنظیمات یک timestamp مشترک برای همهٔ نوع‌ها داشت؛ هر getter دیگر
  (فروشگاه/تم که در هر رندر اجرا می‌شوند) آن را تازه می‌کرد و ردیف قدیمی SMTP
  (با رمز قبلی) «تازه» می‌ماند → تست اتصال با رمز کهنه fail می‌شد، بعد از چند
  ثانیه success، و دوباره fail — دقیقاً رفتار گزارش‌شده.
- رفع: هر نوع کش timestamp خودش را دارد و تنظیمات SMTP اصلاً کش نمی‌شود
  (یک SELECT تک‌ردیفی) — تست و ارسال همیشه با آخرین رمزِ ذخیره‌شده انجام می‌شود.

## ۱۳) پیش‌نمایش زندهٔ فروشگاه = همان قالب فعال
- تنظیمات ← ظاهر و پوسته ← «پیش‌نمایش زندهٔ فروشگاه» دیگر ماکاپ ثابت نیست؛
  قالبِ فعالِ فروشگاه با همان کارت‌های محصول، اسلایدر، استوری و شکل واقعی خودش
  رندر می‌شود (همان دادهٔ صفحهٔ اصلی) و پوسته/حالتِ انتخابی — حتی ذخیره‌نشده —
  زنده روی آن اعمال می‌شود.

## ۱۴) لوگوی فوتر در قالب گیمینگ
- فوتر نوع ۶ (gaming-cyber) تنها variant بدون لوگو بود؛ حالا مثل بقیه،
  footerLogo (→ logo → مونوگرام) بالای ستون نام فروشگاه رندر می‌شود.

## ۱۵) صفحهٔ تعمیر: کارت‌های قشنگ + پیش‌نمایش زنده + تب جدید
- تنظیمات ← فروشگاه ← «قالب و متن‌های صفحه تعمیر»: هر قالب کارت خودش با دکمهٔ
  «پیش‌نمایش زنده» (دیالوگ با همان کامپوننت واقعی + متن‌های همین فرم حتی ذخیره‌نشده)
  و «باز کردن در تب جدید» (مسیر /maintenance-preview?template=… مخصوص مدیر) دارد.

## ۱۶) اسکریپت به‌روزرسانی (Update Script) — تب جدید تنظیمات
- تنظیمات ← «به‌روزرسانی اسکریپت»: آدرس مانیفست (GitHub یا سرور خودتان)،
  «بررسی به‌روزرسانی»، دانلود + نصب خودکار با چک‌سام SHA-256، پشتیبان‌گیری
  خودکار و اجرای db push افزایشی — بدون کوچک‌ترین دست‌زدن به دیتابیس،
  سفارش‌ها، کاربران، محصولات و uploads. راهنمای کامل فارسی: UPDATE-GUIDE.md.

## ۱۷) لوگوی دستیار هوشمند و آواتار مدیر — بدون رفرش
- لوگوی AI: ویجت چت هر ~۶ ثانیه (و روی فوکوس تب) لوگو را از سرور می‌گیرد؛
  ذخیرهٔ لوگوی جدید در تنظیمات ← هوش مصنوعی، همان لحظه روی فروشگاه ظاهر می‌شود.
- آواتار مدیر: ذخیرهٔ پروفایل رویداد taj:admin-avatar منتشر می‌کند و عکس
  سایدبار پنل بلافاصله عوض می‌شود (هم پیش‌فرض، هم آپلودی).

## ۱۸) افزودن مدیران (permissions)
- منوی جدید «مدیران» در پنل: مدیر کل، مدیر کارمندی با ایمیل/رمزِ دلخواهِ خودش
  می‌سازد و با تیک‌های دسترسی مشخص می‌کند به کدام بخش‌ها (محصولات، سفارش‌ها،
  پشتیبانی، …) دسترسی داشته باشد. سایدبارِ کارمند فقط بخش‌های مجاز را نشان
  می‌دهد و APIهای ادمین هم سمت سرور 403 می‌دهند. حذف مدیر = تنزل به کاربر
  عادی (سفارش‌ها و تیکت‌های کارشده دست‌نخورده می‌مانند).

## ۱۹) بازطراحی حرفه‌ای قالب گیمینگ «VICE ARENA»
- با ارجاع به کانسپت GTA VI Vice City و پلتفرم GameUp: هیروی کی‌آرت دختر
  انیمه‌ای وایس‌سیتی (غروب سرخابی + گرید پرسپکتیو)، تایپوگرافی نمایشی ایتالیک،
  دکمه‌های لایمی GTA، سووش‌های نئونی SVG، بنر گرادیانی GameUp با دختر نقره‌ای،
  و مهم‌تر از همه سیستم نور ARGB روی محصولات — حلقهٔ رینگ‌بوی چرخان، زیرگلو
  پالسی، فنِ ARGB که خودِ تصویرش مثل گیف می‌چرخد، کیبورد با نفس‌کشیدن RGB —
  همه فقط با CSS، احترام به prefers-reduced-motion و سوییچ ویژگی‌های قالب.
- تصاویر جدید: vice-girl / stream-girl / argb-rig / argb-fan / argb-keyboard.

## سازگاری و امنیت
- افزوده‌ای (additive): هیچ رفتار v28 تغییر نکرده؛ ستون‌های جدید (adminPermissions،
  updateManifestUrl) با self-heal روی دیتابیس‌های قدیمی خودکار اضافه می‌شوند.
- به‌روزرسانی خودکار هرگز db/ و .env و uploads را لمس نمی‌کند (تست مخرب هم
  اجرا شد: زیپ حاوی db/evil.txt و .env قبل از هر تغییری رد شد).
