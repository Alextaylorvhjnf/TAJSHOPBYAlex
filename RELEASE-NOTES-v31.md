# 👑 تاج الکترونیکس — یادداشت‌های انتشار v31 (نسخهٔ ۲۶.۰.۰)

> **v31 = بازطراحی کامل تجربهٔ کاربری.** این نسخه روی شانه‌های v30 ساخته شده است:
> هیچ داده، API یا رفتار موجود حذف یا تغییر نیافته — همهٔ تغییرات **افزوده‌ای (additive)**
> هستند و دیتابیس، سفارش‌ها، کاربران، محصولات و آپلودها شما کاملاً دست‌نخورده می‌مانند.
>
> 💠 دریافت: پنل مدیریت ← تنظیمات ← به‌روزرسانی اسکریپت ← «بررسی به‌روزرسانی»
> یا روی سرور: `./update.sh` — راهنمای کامل: [`UPDATE-GUIDE.md`](./UPDATE-GUIDE.md)

---

## ۱) 🛠 حالت تعمیر — تب اختصاصی + ۴ کارت بزرگ با پیش‌نمایش زنده

- **تب اختصاصی «حالت تعمیر»** در تنظیمات (جای آن نوارِ فشردهٔ قبلی داخل تب فروشگاه):
  هر قالب یک **کارت بزرگ** با پنه‌لوی تمام‌صفحه دارد که قالب واقعی را با ارتفاع مرورگرِ
  شما مقیاس می‌کند — سایز دقیق، نه باکس ثابت.
- هر کارت دو دکمه دارد: **«پیش‌نمایش زنده»** (دیالوگ با همان کامپوننت واقعی + متن‌های
  همین فرم حتی ذخیره‌نشده) و **«باز کردن در تب جدید»** (`/maintenance-preview?template=…`
  مخصوص مدیر).
- **بازطراحی کامل هر ۴ قالب** (قرارداد داده‌ها دست‌نخورده — متن‌های ذخیره‌شدهٔ قبلی همان‌جا کار می‌کنند):
  - **تِک تاریک (tech-dark):** پنل HUD ترمینالی روی شبکهٔ زمردی، براکت‌های گوشه، LEDهای
    وضعیت، خط اسکن جاروب‌شونده، عنوان گلیچ، لاگ عیب‌یابی مونو (CHECK_DATABASE …
    REPAIR_MODE)، نوارهای پیشرفت درخشان و چیپ‌های تماس ترمینالی.
  - **مینیمال روشن (minimal-light):** سبک اپل — بوم روشن، تیتر عظیم، خط طلایی نازک،
    ورود پلکانی فرم‌ها، ردیف آیکون‌های تماسِ ظریف؛ بدون قاب کارت.
  - **نئون شیشه‌ای (neon-glass):** پس‌زمینهٔ آلوئی تیره با سه اوربِ نئونی شناور، کارت
    شیشه‌ای با حلقهٔ گرادیان کونیکِ چرخان، چیپ لوگوی شناور و تیتر فوشیا→فیروزه‌ای.
  - **شمارش معکوس (countdown-eta):** ETA هوشمند (**۱۸:۰۰ بعدی** — پایدار بین رفرش‌ها)،
    پنجرهٔ پیشرفت ۸ ساعتهٔ پایانی و زیرنویس جدید قابل‌ویرایش `etaNote` برای زمان
    بازگشایی («مثلاً: امروز ساعت ۱۸ به‌روز می‌شویم»).
- تمام واژه‌های هر ۴ قالب مثل قبل از همان فرم قابل ویرایش‌اند؛ مقدار خالی = متنِ طراحی.

## ۲) 🔔 نظرسنجی خودکار به‌روزرسانی + اعلان مدیران + زنگ اعلان‌های پنل

- **API نظرسنجی** (`/api/admin/update/poll`): واخوانی واقعی مانیفست گیت‌هاب با
  محدودسازی ۱۰ دقیقه‌ای (throttle)، فقط برای مدیران؛ به‌محض دیدن نسخهٔ جدید برای
  مدیرهای `ADMIN` و `SUPER_ADMIN` اعلان «به‌روزرسانی جدید نسخهٔ X» می‌سازد
  (بدون تکرار — اعلان خوانده‌نشدهٔ همان نسخه دوباره ساخته نمی‌شود).
- **پنل به‌روزرسانی** حالا هر ۵ دقیقه در پس‌زمینه چک می‌کند و وضعیت «نسخهٔ جدید
  موجود است» را خودکار روی نشان صفحه می‌آورد — بدون کلیک روی «بررسی».
- **زنگ اعلان‌ها (NotificationBell)** در هدر پنل: پاپ‌آور اعلان‌های سیستم با نظرسنجی
  ۲ دقیقه‌ای، نشان شمارندهٔ خوانده‌نشده، «علامت‌گذاری به‌عنوان خوانده‌شده» و ناوبری
  مستقیم به مقصد هر اعلان — کنار شمارنده‌های تیکت و پیام‌ها.
- نتیجه: مدیر بدون باز کردن تب به‌روزرسانی، از نسخهٔ جدید باخبر می‌شود.

## ۳) 🕹 قالب گیمینگ — ریگ گیمینگ کد-تراش با RGB

- **هیروی جدید: برج گیمینگ ۱۰۰٪ کد-تراش** (بدون عکس و بدون اسلایدر): قاب شیشه‌ای
  مات، مادربرد، ماژول‌های RAM، کارت گرافیک، منبع تغذیه و بازتاب کف.
- **۵ فن RGB روشن** (۳ جلو + ۲ داخلی): حلقه‌های رنگین‌کمانیِ چرخان با فاز متفاوت
  هر فن + هالهٔ تنفسی — **طبق خواستهٔ مالک، تیغه‌های فن نمی‌چرخند.**
- **ماوس‌پد ARGB**: حاشیهٔ نورانیِ چرخان (گرادیان کونیک با ماسک)، بافت نقطه‌ای و
  ماوسِ کد-تراش با لوگوی درخشان.
- **هنر سه‌بعدی هدفون‌ها** در دو طرف ریگ: هدفون ARGB صورتی با حلقه‌های رینگ‌بوی
  و هدفون تاکتیکال مشکی با نوارهای نورانی.
- **آواتار کاربر واقعی در هدر گیمینگ**: کاربرِ واردشده آواتار گرد در حلقهٔ نئونی
  بنفش→فوشیا→فیروزه + نام کوچکش را می‌بیند (مهمان = آیکون ورود) — همان هوک
  اشتراکی هدر فروشگاه، کشِ مشترک.
- پولیش هدر/فوتر: خط رینگ‌بوی متحرک زیر نوار تیکت، ارقام مونوی تبوژِ تیکت با متن
  گرادیانی، درخشش فوکِس جستجو، ریتم نئونی دکمه‌ها و ردیف شبکه‌های اجتماعی در فوتر —
  همه فقط `transform/opacity` و همگی زیر کلید `prefers-reduced-motion`.

## ۴) 🔐 صفحات ورود/ثبت‌نام مطابق قالب فعال — ۶ سبک

- صفحه‌های `/login` و `/register` حالا **پوستهٔ هم‌خانوادهٔ قالب فعال فروشگاه** را
  می‌گیرند: **سایبری / شیشه‌ای / لاکچری / روشن / مارکتی / فصلی** — نگاشت کامل هر
  ۲۵ قالب به یکی از این ۶ سبک با رنگ‌های اکسنت اختصاصیِ همان قالب.
- **منطق فرم‌ها ۱:۱ دست‌نخورده** (فیلدها، اعتبارسنجی، مسیرها، توست‌ها) — فقط لایهٔ
  نمایش عوض شده است؛ فرم‌ها از صفحات قبلی عیناً استخراج شده‌اند.
- مدیر می‌تواند با `?template=<id>` (ادمین-محور) پیش‌نمایش هر قالب را روی صفحهٔ
  ورود ببیند؛ کاربر عادی همیشه سبکِ قالب فعال را می‌بیند.

## ۵) 🧭 ۵ سبک مگامنو برای ۲۵ قالب

- پنل مشترکِ دسته‌بندی‌ها حالا **۵ چیدمان متفاوت** دارد (نگاشت کامل روی هر ۲۵ قالب
  + هدر مشترک فروشگاه):
  - **درختی دیجی‌کالایی** (tree): ریل آیکونی + پنل شاخه‌ها
  - **تصویری** (images): کاشی‌های عکس‌دار دسته‌ها + برندها
  - **آبشاری** (waterfall): ستون‌های پلکانی
  - **زوم-محو** (zoomfade): ریل جمع‌شونده با بزرگ‌نمایی محتوا
  - **آبشاری + محصول** (waterfall+product): آبشار + پنل «پیشنهاد ویژه» با محصول
    پربازدهِ همان لحظه (از همان دادهٔ صفحه، بدون درخواست جدید)
- همان رفتار حرفه‌ای قبلی: hover-intent، ESC/فوکِس/اسکرول، بستنِ انیمیت‌شده، هدف‌های
  لمسی ۴۴px، پشتیبانی کامل RTL و حالت روشن/تاریک.

## ۶) 🧩 محتوای اختصاصی هر قالب (۲۵ قالب)

- بخش جدید **«محتوای اختصاصی قالب»** در پنل (ظاهر و پوسته): برای هر قالب،
  **اسلایدها / شوکیس‌ها / متن‌ها / لینک‌ها / برندِ مخصوص همان قالب** را تعریف کنید —
  ۵ تب با بارگذاری تصویر، مرتب‌سازی، چیپ‌های آماده و نشان تعداد مقادیر سفارشی.
- **اولویت با قالب است، جای خالی = مقدار سراسری**: اسلاید/شوکیس/برند قالب فقط در
  زمان فعال‌بودن همان قالب جای مقادیر سراسری را می‌گیرد و برند قالب روی هدر/فوتر
  صفحات داخلی هم اعمال می‌شود. همهٔ ۲۵ قالب این رفتار را بدون تغییر کد خودشان
  می‌گیرند (ادغام سمت سرور در یک نقطه).
- اعتبارسنجی سخت‌گیر Zod + پیام‌های فارسی + پاک‌سازی خودکار مقادیر نامعتبر؛ ثبت
  هر تغییر در گزارش فعالیت مدیران.

## ۷) 🔒 حفاظت کامل داده‌های کاربران در به‌روزرسانی

- کانال به‌روزرسانی (پنل و `update.sh`) **هرگز** به این‌ها دست نمی‌زند:
  - `db/` — سفارش‌ها، کاربران، محصولات، تنظیمات (volume `taj_db`)
  - `.env` — کلیدها و درگاه پرداخت
  - `uploads/` / `public/uploads/` — تصاویر بارگذاری‌شده (volume `taj_uploads`)
- ZIP فقط مسیرهای مجاز را بازنویسی می‌کند (`src/` ، `public/` منهای uploads،
  `prisma/` ، `scripts/` و فایل‌های پیکربندی)؛ چک‌سام **SHA-256** قبل از اعمال
  بررسی می‌شود، از فایل‌های فعلی در `db/update-backup-…` پشتیبان گرفته می‌شود و
  مهاجرت دیتابیس فقط با `prisma db push` غیرمخرب انجام می‌شود.
- `minAppVersion` این نسخه **۲۴.۰.۰** است — همهٔ نصب‌های ۲۴ به بالا این به‌روزرسانی
  را دریافت می‌کنند.

---

## 🚀 نصب تازه (یک خط) / به‌روزرسانی

| روش | دستور |
|---|---|
| **نصب تازه با بستهٔ کامل** | ZIP کامل پروژه را در ریشهٔ سرور آپلود کنید و: `sudo bash -c 'unzip -oq "Taj Electronics Main V13 VC.zip" && bash install.sh'` — سپس `http://SERVER_IP:3000/install` |
| **سرور خام (جایگزین)** | `sudo ./setup.sh` (Docker + Compose + Node 22 + Bun + OpenSSL) بعد `sudo ./install.sh` |
| **به‌روزرسانی از پنل** | تنظیمات ← به‌روزرسانی اسکریپت ← «بررسی به‌روزرسانی» ← «دانلود و نصب» |
| **به‌روزرسانی از ترمینال** | `./update.sh` (یا `--check` / `--force`) |

## سازگاری

- افزوده‌ای و بدون تخریب: تنها مدل جدید دیتابیس `TemplateContent` است (یک ردیف
  JSON به‌ازای هر قالب) — `prisma db push` کافی است و self-heal اجرای زمان-اجرای
  اسکیمای قدیمی هم آن را خودکار اضافه می‌کند.
- رفتار همهٔ مسیرها و APIهای موجود v30 بدون تغییر؛ قالب فعال، تنظیمات و داده‌های
  شما بعد از به‌روزرسانی دقیقاً سر جای خودشان هستند.
