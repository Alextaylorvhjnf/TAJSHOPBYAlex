# یادداشت انتشار v32 — تاج الکترونیکس (نسخهٔ داخلی ۲۷.۰.۰)

انتشار v32 بر پایهٔ v31 (۲۶.۰.۰) — همهٔ داده‌های کاربر (دیتابیس، .env، آپلودها) دست‌نخورده و قابل انتقال.

---

## ۱) بخش به‌روزرسانی اسکریپت — بدون هیچ تنظیمی
- اتصال **دائمی و ثابت** به مخزن گیت‌هاب شما (TAJSHOPBYAlex) — ورودی «آدرس مانیفست» کلاً حذف شد؛ هیچ لینکی نباید وارد یا ذخیره شود.
- فقط **یک دکمهٔ «بررسی به‌روزرسانی»** → fetch مستقیم از گیت‌هاب → اگر به‌روز باشید: «شما از آخرین نسخهٔ اسکریپت استفاده می‌کنید»؛ اگر نسخهٔ جدید بود: **changelog ساختاریافته** + دکمهٔ «نصب نسخه جدید».
- جریان ایمن نصب: تیک «قبل از به‌روزرسانی فول بکاپ می‌خواهم» → چک‌لیست ۵ مرحله‌ای با نوار پیشرفت (اطلاعات فروشگاه، مشتریان، برندها، محصولات، بسته‌بندی) → **دانلود ZIP بکاپ** روی سیستم شما → «بکاپ را دانلود کردم» → اجرای به‌روزرسانی → پیام «اسکریپت شما به آخرین نسخه به‌روز شد» + دکمهٔ **«رفرش صفحه»**.
- **Polling خودکار**: اسکریپت به‌طور دوره‌ای به گیت‌هاب وصل می‌شود؛ هر ورژن جدیدی منتشر شود، همان لحظه در **زنگ اعلان‌ها (اعلان‌ها و پیام‌ها)** نوتیفیکیشن می‌آید: «آپدیت جدید اسکریپت موجود است، می‌توانید آپدیت کنید» — کلیک روی آن مستقیم به صفحهٔ به‌روزرسانی می‌رود.

## ۲) پنل مدیریت — بازطراحی کامل (مطابق طرح‌های ارسالی شما)
- سایدبار و نوار بالا با کارت‌های گردگوشه، گروه‌بندی منوها، آیتم فعال با پیل گرادیانی — دقیقاً به سبک داشبوردهای مرجع (RTL فارسی).
- **۴ تم رنگی انتخابی**: بنفش / صورتی / زغالی-طلایی / قرمز + حالت روشن/تاریک — متن‌ها در همهٔ تم‌ها کاملاً خوانا.
- داشبورد جدید: بنر خوشامد «سلام {نام مدیر} 👋 / نمایش کلی و لحظه‌ای عملکرد فروشگاه» (با دکمهٔ بستن تا آپدیت بعدی)، ۴ کارت KPI (فروش کل، سفارش‌ها، مشتریان جدید، پرداخت‌شده) با اسپارک‌لاین، نمودار Area/Bar/Donut، پنل جزئیات سفارش شیشه‌ای، لیست سفارش‌های اخیر و محصولات پرفروش — همه با اعداد فارسی.
- **دادهٔ نمونه واقعی** در هر دو دیتابیس (dev + نصب تازه): ~۴۰ مشتری و ~۱۲۰ سفارش ۶۰ روز اخیر تا نمودارها زنده باشند (با اسکریپت `scripts/seed-demo-data.mjs`، idempotent).

## ۳) محتوای اختصاصی قالب‌ها — صفحهٔ مستقل + پرشده با پیش‌فرض‌ها
- بخش «محتوای قالب‌ها» حالا آیتم مستقل منوی ادمین است (کنار «تغییر قالب»)، نه مخفی در صفحهٔ ظاهر.
- ویرایشگر هرگز خالی نیست: **مقادیر طراحی‌شدهٔ هر قالب از قبل پر شده** + برچسب «پیش‌فرض» + توضیح «این مقدار در کدام بخش استفاده می‌شود» + پیش‌نمایش زندهٔ مینی.
- گیمینگ: تیتر/زیرتیتر هیرو، آرنا، تجهیزات، منطقهٔ تخفیف، بخش عضویت (کوپایلت)، دکمه و لینک شروع چت، تصویر ویجت AI — همه از بک‌اند قابل ویرایش.
- **اسلایدها**: شمارش معکوس اختصاصی هر اسلاید (تیک + تاریخ + برچسب) که در فرانت به‌شکل زیبا نمایش داده می‌شود + پشتیبانی **GIF** (انیمیشن حفظ می‌شود) + **ویدئو تا ۱۰۰ مگابایت** (پخش خودکار بی‌صدا).

## ۴) حالت تعمیر ۲.۰
- ۴ کارت انتخاب قالب حالا **مربع‌های کوچک جمع‌وجور** با پیش‌نمایش زندهٔ واقعی + کادر طلایی انتخاب.
- صفحات تعمیر **کاملاً ریسپانسیو تک‌صفحه‌ای**: در موبایل، لپ‌تاپ و مانیتور بزرگ بدون اسکرول در یک صفحه جا می‌شوند.
- **لوگوی اختصاصی**: آپلود لوگو برای صفحهٔ تعمیر (fallback: لوگوی فروشگاه).
- قالب شمارش معکوس: **تنظیم زمان (روز/ساعت)** از پنل.
- بازطراحی هر ۴ قالب (به‌ویژه مینیمال روشن → ادیتوریال پرمیوم).

## ۵) رفع اشکالات
- **آپلود آواتار مدیر**: دیگر نیازی به «حذف آواتار» نیست — هر بار کلیک = تعویض؛ پیش‌نمایش با نام و حجم فایل.
- **لوکس الکترونیکس**: ریشه‌یابی و رفع باگ CSS که اسلایدر/شوکیس/دسته‌بندی‌ها را نامرئی می‌کرد (ماسک روی خود المان) + اسلایدر جدید Ken-Burns با نوارهای پیشرفت طلایی.
- **گیمینگ**: منوی هدر بدون نیاز به سوایپ — همهٔ آیتم‌ها (خانه/فروشگاه/دسته‌بندی‌ها/…) یک‌جا و کامل نمایان.

## ۶) قالب‌ها
- **گیمینگ و سایبر**: بازطراحی کامل حرفه‌ای (پالت بنفش/صورتی/لایم، HUD، نشان‌های LIVE، کیس RGB کدنویسی‌شده حفظ شد) + ۱۷ فیلد محتوای بک‌اند.
- **موبایل‌محور حذف شد** → «پرمیوم ریسپانسیو»: قالب جدید ریسپانسیو برای همهٔ دستگاه‌ها.
- **سیستم انیمیشن اسکرول (scroll-fx)** مشترک: ورق‌خوردن سه‌بعدی مثل کتاب، پارالاکس نوار، روشن‌شدن چراغ‌ها با اسکرول — اعمال‌شده در پرمیوم ریسپانسیو، کاتالوگ چاپی و سوپراستور.
- **ویجت هوش مصنوعی**: آواتار قالب‌محور (ربات Concierge برای عموم، آرکید برای گیمینگ) + قابل تعویض از بک‌اند.

## ۷) سازندهٔ هدر/فوتر و افکت‌های محصول
- **۴ پوستهٔ هدر**: کلاسیک / کریستالی / لیکوئید گلس / مینیمال — انتخاب از بخش «هدر و فوتر».
- **چینش منوی هدر با درگ** (خانه، فروشگاه، درباره ما، …) + جای‌گذاری حالت روشن/تاریک، سبد و پروفایل.
- **افکت‌های هاور محصولات** با پیش‌نمایش زنده: چرخش سه‌بعدی / از بالا به پایین / چپ به راست / محو-ظهور / بزرگنمایی.

## ۸) دسته‌بندی‌ها و کاتالوگ
- **مگامنوی جمع‌وجور**: همهٔ دسته‌بندی‌ها + تصاویر در یک پنل محدود بدون اسکرول طولانی.
- **آیکون‌های دسته‌بندی**: موبایل، لپ‌تاپ، کامپیوتر، CPU، مانیتور، کیبورد، ماوس، هدفون، هندزفری، ساعت، دوربین، کنسول و…
- **محصولات با تصویر منطبق**: کاتالوگ با تصاویر تأییدشدهٔ اختصاصی هر کالا (رفع کامل عکس‌های ناهمخوان مثل اسپیکر JBL با عکس بستهٔ پستی) — رشد کاتالوگ با اسکریپت idempotent (`scripts/seed-products-v32.mjs`).

## ۹) سازگاری و نصب
- همان نصب یک‌خطی: `sudo bash -c 'unzip -oq "Taj Electronics Main V13 VC.zip" && bash install.sh'`
- به‌روزرسانی از v24+: همان بخش به‌روزرسانی — کانال گیت‌هاب، بکاپ کامل، نصب امن.
- داده‌ها، قالب فعال، تنظیمات و آپلودها همگی حفظ می‌شوند.
