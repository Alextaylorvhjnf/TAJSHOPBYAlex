# یادداشت انتشار v33 — تاج الکترونیکس (نسخهٔ داخلی ۲۸.۰.۰)

انتشار v33 بر پایهٔ v32 — همهٔ داده‌های کاربر (دیتابیس، .env، آپلودها) دست‌نخورده باقی می‌مانند. به‌روزرسانی مثل همیشه از پنل ادمین (بخش «به‌روزرسانی اسکریپت») یا `./update.sh` انجام می‌شود و اسکیمای دیتابیس به‌صورت خودکار و غیرمخرب sync می‌شود.

---

## ۱) 🤖 ربات تلگرام فروشگاهی — کامل و حرفه‌ای

یک فروشگاه کامل داخل تلگرام، متصل به همان دیتابیس و تنظیمات سایت شما. هیچ فایل قدیمی تغییری نکرده — کل موتور ربات داخل پروسهٔ خود Next.js اجرا می‌شود (بدون سرویس جانبی، بدون نیاز به وب‌هوک و دامنه).

### راه‌اندازی (تنها از پنل ادمین)
- **تنظیمات → تب جدید «ربات تلگرامی»**: وارد کردن **توکن ربات** (از @BotFather)، **شناسه چت ادمین**، پیام خوش‌آمد دلخواه و کلید فعال‌سازی.
- دکمهٔ **«تست اتصال»** با گزارش دقیق فارسی (توکن نامعتبر / قطع شبکه / موفق + ارسال پیام آزمایشی به چت ادمین).
- **کارت وضعیت اتصال زنده**: متصل / خطا / خاموش + آخرین خطا و زمان‌ها.
- راهنمای گام‌به‌گام ۶ مرحله‌ای + لیست قابلیت‌ها، همه داخل همین تب.
- 🔒 توکن هرگز به مرورگر برگردانده نمی‌شود (فقط ماسک‌شده نمایش داده می‌شود).

### فروشگاه داخل ربات (برای مشتریان)
- `/start` → منوی اصلی دائمی (کیبورد ثابت پایین چت) + کارت‌های شیشه‌ای.
- **کاتالوگ کامل**: همهٔ دسته‌بندی‌ها با تعداد کالا، همهٔ محصولات، تازه‌ها، تخفیف‌دارها و ویژه‌ها با صفحه‌بندی.
- **کارت محصول**: عکس (آپلود multipart — حتی بدون دامنهٔ عمومی کار می‌کند)، قیمت/تخفیف درصدی، موجودی، امتیاز، مشخصات کلیدی، رنگ‌بندی، دکمهٔ «مشاهده در وب‌سایت».
- **جستجوی ساده** + **مشاور خرید هوش مصنوعی** 🤖: همان موتور AI سایت (با ابزارهای جستجوی واقعی کاتالوگ و پیگیری سفارش) داخل تلگرام — سوال بپرسید: «گوشی زیر ۲۰ میلیون برای گیم چه دارید؟»
- **مقایسهٔ دو محصول** ⚖️: انتخاب از دکمهٔ «مقایسه با…» → جدول قیمت/موجودی/امتیاز/مشخصات.
- **سبد خرید**: افزودن (با انتخاب رنگ)، تغییر تعداد، حذف، خالی‌کردن.
- **ثبت سفارش ۷ قدمی** (نام، موبایل، استان، شهر، نشانی، کدپستی اختیاری، یادداشت اختیاری) — با دکمهٔ «از اطلاعات حساب من استفاده کن» برای حساب‌های متصل.
- **پرداخت — دقیقاً همان روش‌های فعال سایت**: درگاه **زرین‌پال** (دکمهٔ پرداخت با لینک StartPay، تاییدیهٔ پرداخت بعداً همان‌جا ارسال می‌شود) یا **کارت به کارت** (اطلاعات کارت + **ارسال عکس رسید** داخل چت → بررسی مدیر → تایید).
- پیام موفقیت: «سفارش شما با موفقیت ثبت شد ✅ شناسهٔ پیگیری TAJ-…».
- **پیگیری سفارش** با شمارهٔ سفارش (یا لیست سفارش‌های حساب متصل).
- **پشتیبانی و تیکت** 🎫: ثبت تیکت جدید از تلگرام، دیدن گفتگو، پاسخ دادن — پاسخ پشتیبانی همان لحظه در چت مشتری می‌رسد.
- **اتصال حساب سایت** با موبایل + رمز عبور (برای تیکت، پیگیری و پیش‌پر کردن اطلاعات).
- **اطلاع‌رسانی محصولات جدید** 🆕: هر محصول جدیدی که منتشر شود، برای مشترکین علاقه‌مند (قابل خاموش‌کردن) با عکس و دکمهٔ «مشاهده و خرید» ارسال می‌شود.

### پنل مدیریت داخل ربات (برای ادمین)
از منوی «🛠 پنل مدیریت» (فقط برای شناسه‌های چت ادمین):
- **آمار زنده**: سفارش‌های فعال، رسیدهای در انتظار، تیکت‌های باز، فروش امروز/کل/۳۰روزه.
- **مدیریت سفارش‌ها**: لیست با ۸ فیلتر وضعیت، جزئیات کامل، **تغییر وضعیت** (پرداخت‌شده/در پردازش/ارسال‌شده/تحویل/لغو/…) با یک لمس، **افزودن یادداشت** به سفارش.
- **رسیدهای کارت به کارت**: کارت عکس رسید + جزئیات واریز + دکمه‌های **«تایید»/«رد»** — تایید یعنی همان منطق سایت (علامت‌گذاری پرداخت‌شده، کم‌کردن موجودی، نوتیفیکیشن به مشتری).
- **تیکت‌ها**: لیست تیکت‌های باز، مشاهدهٔ گفتگو، **پاسخ از خود تلگرام**، بستن/بازگشایی.
- **پیام همگانی** 📣: ارسال متن به همهٔ مشترکین ربات.

### اطلاع‌رسانی خودکار (بدون دست‌زدن به کدهای سایت)
موتور Watcher هر ۱۲ ثانیه دیتابیس را رصد می‌کند: تغییر وضعیت سفارش → پیام به مشتری؛ تایید پرداخت زرین‌پال → رسید تاییدیه؛ تایید/رد رسید کارت‌به‌کارت → اطلاع مشتری؛ پاسخ جدید تیکت → پیام به طرف مقابل؛ محصول جدید → اطلاع‌رسانی عمومی.

### ⚠️ نکتهٔ مهم برای سرورهای ایران (با احترام و مؤدبانه)
اگر فروشگاه شما روی سروری **داخل ایران** میزبانی شود، به دلیل فیلترینگ تلگرام در ایران اتصال ربات برقرار نمی‌شود و این قابلیت قابل استفاده نیست. جهت استفاده از ربات لطفاً فروشگاه را روی یک **سرور خارجی** میزبانی کنید؛ سرورهای **ترکیه، آلمان و انگلیس** گزینه‌های مناسب با پینگ و سرعت خوب هستند. این پیام هم در تب تنظیمات ربات و هم در خود ربات برای کاربران قابل مشاهده است.

---

## ۲) 🎯 اصلاح لینک‌های اسلایدر و «مشاهده همه» — فیلتر واقعی

مشکل: دکمه‌های اسلایدر («مشاهدهٔ گوشی‌ها»، «ورود به منطقهٔ گیمینگ» و…) و دکمه‌های «مشاهدهٔ همه» بالای ردیف‌های صفحهٔ اول، همه به لیست «همهٔ محصولات» می‌رفتند.

اصلاح شد:
- **اسلایدرها**: «مشاهدهٔ گوشی‌ها» → فقط گوشی‌ها (دستهٔ موبایل)؛ «مشاهدهٔ لپ‌تاپ‌ها» → فقط لپ‌تاپ‌ها؛ «ورود به منطقهٔ گیمینگ» → محصولات گیمینگ؛ و الگو برای هر متن دکمه (ساعت، هدفون، مانیتور، تخفیف…). تشخیص هوشمند از روی متن دکمه/عنوان/بج انجام می‌شود و لینک اختصاصی ادمین همیشه محترم شمرده می‌شود. دیتابیس‌های نصب تازه و فعلی هم اصلاح شدند.
- **ردیف‌های صفحهٔ اول**: «مشاهدهٔ همه» هر ردیف فقط محصولات همان ردیف را نشان می‌دهد — تخفیف‌دارها → فقط تخفیف‌دارها (`?discount=1`)، ویژه‌ها → فقط ویژه‌ها (`?special=1`)، پیشنهادهای ویژه → `?featured=1`، پرفروش‌ها → مرتب‌شده با پرفروش‌ترین، تازه‌ها → جدیدترین‌ها. این اصلاح روی **همهٔ ۲۳ قالب** فروشگاه اعمال شد.
- صفحهٔ محصولات حالا پارامترهای `?featured=1` و `?special=1` را می‌فهمد و عنوان فارسی اختصاصی می‌گیرد («پیشنهادهای ویژه»، «محصولات ویژه و انحصاری»، «محصولات تخفیف‌دار»)؛ غلط املایی `sort=bestseller` هم پذیرفته و نگاشت می‌شود.

---

## ۳) جزئیات فنی
- **۴۵ تست یکپارچه** روی موتور ربات (فلوی کامل خرید، رسید عکسی، تایید مدیر، اتصال حساب، تیکت‌ها، پنل ادمین) — همه سبز.
- موتور ربات: `src/lib/telegram/*` (api, types, common, views, flows, ai-chat, tickets-bot, admin, watcher, poller) + `src/instrumentation.ts` — بدون وابستگی جدید، بدون تغییر در Dockerfile/entrypoint (پروسهٔ خود سرور).
- اسکیمای جدید: `TelegramBotSettings` + `TelegramSubscriber` (به‌صورت غیرمخرب اضافه می‌شود؛ `prisma db push` خودکار هنگام بوت).
- QA مرورگر: صفحهٔ اصلی، صفحات فیلتردار (موبایل/تخفیف/ویژه/پیشنهاد)، تب «ربات تلگرامی» (رندر، ذخیره، تست اتصال، ماسک توکن)، ریسپانسیو ۳۹۰px بدون اسکرول افقی، فوتر چسبیده — همه سبز، بدون خطای کنسول.
