# یادداشت انتشار v34 — تاج الکترونیکس (نسخهٔ داخلی ۲۹.۰.۰)

این نسخه فقط یک هدف دارد: **نصب فوق‌سریع و فوق‌سبک** — بدون تغییر در هیچ قابلیتی از فروشگاه.

---

## ۱) 🚀 نصاب جدید — بدون Docker، بدون بیلد روی سرور شما

### مشکل نسخه‌های قبلی
تا v33 نصب از Docker استفاده می‌کرد: روی **سرور خودتان** باید Docker نصب می‌شد، همهٔ وابستگی‌ها دانلود می‌شد و کل فروشگاه از صفر بیلد می‌شد:
- ⏱ ۳۰ تا ۴۰ دقیقه زمان (یا بیشتر)
- 🧠 مصرف شدید RAM (در سرورهای ضعیف تا ۸۰٪+ و قفل‌شدن سرور)
- 💾 تا ~۱۰ گیگابایت فضای دیسک (لایه‌های Docker + node_modules + کش بیلد)

### راه‌حل v34
بیلد Next.js **از قبل انجام شده** و داخل بسته (پوشهٔ `runtime/`) آماده است. نصاب جدید فقط آن را کپی می‌کند:

| | نسخه‌های قبلی (Docker) | **v34** |
|---|---|---|
| زمان نصب | ۳۰-۴۰+ دقیقه | **~۲ تا ۴ دقیقه** |
| بیلد روی سرور | بله (کامل) | **هیچ** |
| مصرف RAM در نصب | شدید | **تقریباً هیچ** |
| فضای دیسک | تا ~۱۰GB | **زیر ۱GB** |
| درگاه نصب | پورت 3000 | **پورت 80/443 با Nginx** |

### مراحل نصب (همه خودکار)
1. **پیش‌نیازها** — Node.js 22 + Nginx + certbot فقط در صورت نبود نصب می‌شوند
2. **دامنه** — اسکریپت از شما می‌پرسد؛ DNS را چک می‌کند
3. **استقرار** — برنامهٔ آماده در `/var/www/taj-electronics` (بدون کامپایل)
4. **تنظیمات** — `.env` با رمز تصادفی + دیتابیس دمو (۱۰۰ محصول) نصب می‌شود
5. **Nginx** — ریورس‌پروکسی + gzip + تنظیم آپلود ۶۴MB به‌صورت خودکار
6. **SSL** — گواهی رایگان Let's Encrypt (فقط با دامنهٔ معتبر؛ تمدید خودکار)
7. **سرویس systemd** — اجرای خودکار بعد از ری‌بوت + بررسی سلامت + گزارش نهایی

### دستور نصب (همان یک خط همیشگی)
فایل ZIP را در روت سرور آپلود کنید و:
```bash
sudo bash -c 'command -v unzip >/dev/null || (apt-get update -qq && apt-get install -y -qq unzip) 2>/dev/null || dnf install -y -q unzip; unzip -oq "Taj Electronics Main V13 VC.zip" && bash install.sh'
```
بعد از پایان: `http://دامنه-یا-IP/install` را باز کنید و مدیر کل بسازید.

### قابلیت‌های نصاب
- **تعاملی**: دامنه را می‌پرسد (Enter = نصب فقط با IP)
- **تشخیص DNS**: اگر دامنه به IP دیگری اشاره کند، با پیام روشن ادامه می‌دهد و بعد از اصلاح DNS با اجرای مجدد، SSL فعال می‌شود
- **اگر نصب Docker قبلی** روی سرور باشد: خودش آن را متوقف و داده‌های دیتابیس/آپلودها را منتقل می‌کند
- **اجرای مجدد = ارتقا**: `sudo bash install.sh` دوباره، کد را عوض و داده‌ها را حفظ می‌کند
- **گزارش نهایی**: زمان دقیق نصب، حجم و RAM مصرفی، دستورات مدیریت
- **اگر سرور ایران باشد**: با احترام اعلام می‌کند ربات تلگرام روی سرور ایران قابل استفاده نیست (فیلترینگ) و سرور ترکیه/آلمان/انگلیس پیشنهاد می‌شود — بقیهٔ فروشگاه معمولی کار می‌کند
- پاک‌سازی پیشنهادی ایمیج‌های Docker قدیمی (~۱۰GB آزاد می‌شود) در پایان گزارش داده می‌شود

---

## ۲) 🔄 به‌روزرسانی‌ها هم سریع شدند
`update.sh` جدید هر دو حالت را خودکار تشخیص می‌دهد:
- **نصب Standalone (v34+)**: بستهٔ به‌روزرسانی جدید شامل برنامهٔ از پیش ساخته‌شده است → تعویض فایل + ری‌استارت — **بدون بیلد، حدود ۱ دقیقه**. دیتابیس، آپلودها و `.env` دست‌نخورده.
- **نصب Docker (نسخه‌های قدیمی)**: دقیقاً مثل قبل (build + up).
- دیتابیس در هر ری‌استارت به‌صورت غیرمخرب با `prisma db push` همگام می‌شود.

---

## ۳) 🔧 جزئیات فنی
- پوشهٔ `runtime/` داخل بسته = خروجی رسمی `next build` با `output: "standalone"` + تمام وابستگی‌های اجرایی (Node modules ردیابی‌شده) + Prisma CLI (برای `db push`) + دیتابیس دمو + اسکریپت بوت (seed → همگام‌سازی اسکیما → اجرا).
- موتورهای Prisma برای **هر خانوادهٔ سیستم‌عامل** تولید شده‌اند: Debian/Ubuntu، RHEL/AlmaLinux/Rocky و Alpine — بسته روی هر سرور لینوکسی رایج اجرا می‌شود.
- سرویس systemd: `systemctl status|restart taj-electronics`، لاگ زنده: `journalctl -u taj-electronics -f`، اجرای خودکار بعد از ری‌بوت.
- **هیچ فایل کد فروشگاه تغییر نکرده** — همهٔ قابلیت‌های v33 (فروشگاه، ۲۳ قالب، ربات تلگرام کامل، زرین‌پال، کارت‌به‌کارت، تیکت، هوش مصنوعی و…) عیناً همان‌ها هستند. تغییرات فقط: `prisma/schema.prisma` (افزودن binaryTargets — افزایشی)، `install.sh` (بازنویسی)، `update.sh` (حالت standalone)، `scripts/release-update.mjs` (ساخت بستهٔ به‌روزرسانی حاوی برنامهٔ آماده).
- مسیر Docker همچنان موجود است (`docker-compose.yml` + `Dockerfile` دست‌نخورده) — هر کس بخواهد می‌تواند مثل قبل با Docker نصب کند.
