# یادداشت انتشار v34.2 — تاج الکترونیکس (نسخهٔ داخلی ۲۹.۰.۲)

این نسخه **ریشهٔ خطای 404 «تست اتصال» هوش مصنوعی** را به‌طور کامل رفع می‌کند و مکانیزم به‌روزرسانی پنل را برای نصب‌های استندالون کامل می‌کند.

---

## ۱) 🐛 علت خطای 404 «تست اتصال»

مسیر API تست اتصال (`/api/ai/test`) در **کد کامپایل‌شده** (`runtime/.next`) نسخه‌های قدیمی وجود نداشت.
به‌روزرسانی قبلی (۲۹.۰.۱) فقط **کد منبع** (`src/`) را جایگزین می‌کرد — ولی فروشگاه شما از
برنامهٔ **از پیش ساخته‌شده** اجرا می‌شود؛ یعنی `‎.next` قدیمی همچنان سرو می‌شد و هر مسیر جدید
(از جمله تست اتصال) خطای 404 می‌داد. این یعنی دکمهٔ «تست اتصال» بعد از نصب ۲۹.۰.۱ هم به
برنامهٔ قدیمیِ کامپایل‌شده می‌خورد.

## ۲) 🔧 راه‌حل — تعویض کامل برنامهٔ کامپایل‌شده از خود پنل

از این نسخه به بعد، به‌روزرسانِ داخل پنل تشخیص می‌دهد که نصب شما «استندالون» است
(`server.js` + `‎.next`) و **خودش برنامهٔ کامپایل‌شده را هم تعویض می‌کند** — بدون هیچ بیلدی
روی سرور:

- `‎.next/` کامل جایگزین می‌شود (کپی در محل موقت + دو rename اتمیک — قطعی سرویس فقط چند میلی‌ثانیه)
- `server.js`، `‎.version`، `update.sh`، `db-seed/` به‌روز می‌شوند
- **کلاینت Prisma** همراه بسته ارسال می‌شود: `node_modules/.prisma/client` با موتورهای
  کوئری برای همهٔ پلتفرم‌های لینوکس (Debian/Ubuntu و RHEL — openssl 3 و 1.1) + `@prisma/client`
  → تغییرات اسکیما (مثل فیلدهای جدید) بی‌درنگ کار می‌کنند
- کپی فایل‌ها **اتمیک** انجام می‌شود (write + rename) تا فرآیندهای در حال اجرا (مثل موتور
  mmap شدهٔ Prisma) هرگز وسط به‌روزرسانی کرش نکنند — حتی وقتی پنل خودش در همان لحظه
  status می‌گیرد
- نسخهٔ قبلی کد در `db/update-backup-<timestamp>/` نگه داشته می‌شود

داده‌ها (دیتابیس، `public/uploads/`، `.env`) مثل همیشه **هرگز دست نمی‌خورند**.

## ۳) ⚠️ یک نکتهٔ مهم برای همین به‌روزرسانی

کد به‌روزرسانِ جدید داخل `‎.next` **جدید** کامپایل شده — و `‎.next` فعلی شما هنوز نسخهٔ
قدیمی است (خودش نمی‌تواند خودش را تعویض کند!). بنابراین برای **اولین بار**:

```bash
cd /var/www/taj-electronics && bash update.sh
```

اسکریپت ابتدا خودش را از گیت‌هاب ارتقا می‌دهد، بعد برنامهٔ کامپایل‌شده را کامل تعویض می‌کند
(بدون بیلد، حدود ۱ دقیقه). **از آن به بعد** دکمهٔ «نصب نسخه جدید» در پنل (
تنظیمات ← اسکریپت به‌روزرسانی) همه‌چیز را خودش انجام می‌دهد.

## ۴) سایر بهبودها

- برچسب «standalone» در پنل حالت نصب را درست تشخیص می‌دهد و پیغام راهنما به‌روز شد
- پیغام‌های مرحله‌ای به‌روزرسانی حالا تعویض runtime را هم گزارش می‌کنند («تعویض برنامهٔ
  کامپایل‌شده…»)
- در بستهٔ ZIP: حذف `bun.lock` از ریشه (سازگاری با اعتبارسنج پنل)، موتور musl حذف شد تا
  حجم زیر سقف ۱۰۰MB گیت‌هاب بماند
