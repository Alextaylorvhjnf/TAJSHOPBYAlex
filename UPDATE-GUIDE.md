# راهنمای به‌روزرسانی اسکریپت — تاج الکترونیکس (Update Script)

این راهنما نحوهٔ استفاده از بخش **«اسکریپت به‌روزرسانی»** پنل مدیریت را توضیح می‌دهد: چطور نسخهٔ جدید را منتشر کنید، مانیفست را بسازید، فایل ZIP را آماده کنید و فروشگاه خودتان را بدون از دست رفتن حتی یک رکورد داده به‌روز نگه دارید.

- نسخهٔ فعلی اسکریپت از فیلد `version` فایل `package.json` خوانده می‌شود.
- تنظیمات این بخش در مسیر **پنل مدیریت → تنظیمات → اسکریپت به‌روزرسانی** است.
- هیچ فایل داده‌ای (`db/`، `.env`، `uploads/`) هرگز تغییر نمی‌کند.

---

## ۱) قالب مانیفست (update-manifest.json)

مانیفست یک فایل JSON کوچک است که به اسکریپت می‌گوید «آخرین نسخه چیست و فایل آن کجاست». نمونه:

```json
{
  "version": "29.3.0",
  "releasedAt": "2026-09-08T12:00:00Z",
  "notes": "توضیحات نسخه — تغییرات قالب‌ها و امکانات جدید",
  "zipUrl": "https://raw.githubusercontent.com/USER/REPO/main/updates/taj-electronics-29.3.0.zip",
  "sha256": "a1b2c3d4e5f6... (۶۴ کاراکتر هگز — اختیاری اما به‌شدت توصیه‌شده)",
  "minAppVersion": "29.0.0"
}
```

| فیلد | الزامی؟ | توضیح |
|---|---|---|
| `version` | ✔ | شمارهٔ نسخهٔ جدید (مثل `29.3.0`). اگر از نسخهٔ فعلی `package.json` بزرگ‌تر باشد، «به‌روزرسانی در دسترس» اعلام می‌شود. |
| `releasedAt` | — | تاریخ انتشار (ISO 8601). فقط برای نمایش. |
| `notes` | — | یادداشت‌های نسخه؛ در کارت نتیجهٔ بررسی نمایش داده می‌شود (خطوط جدید پشتیبانی می‌شود). |
| `zipUrl` | ✔ (برای نصب) | آدرس مستقیم فایل ZIP. لینک‌های GitHub Releases (که ریدایرکت می‌شوند) پشتیبانی می‌شوند. اگر خالی باشد فقط اعلان نسخهٔ جدید نمایش داده می‌شود و نصب ممکن نیست. |
| `sha256` | ✔ (توصیه‌شده) | چک‌سام SHA-256 فایل ZIP. اگر ارائه شود، قبل از اعمال هر تغییری صحت فایل تأیید می‌شود و در صورت مغایرت همه‌چیز لغو می‌شود. |
| `minAppVersion` | — | حداقل نسخه‌ای که باید فعلاً نصب باشد تا این بسته قابل نصب باشد (مثلاً پرش از نسخه‌های خیلی قدیمی ممنوع شود). |

> نکته: اسکریپت به‌روزرسان یک alias کوچک هم می‌پذیرد — اگر `zipUrl` نبود ولی `releaseAsset` (آدرس http/https) بود، همان به‌عنوان آدرس ZIP استفاده می‌شود. قالب رسمی همان `zipUrl` است.

اولویت آدرس مانیفست (وقتی دکمهٔ «بررسی به‌روزرسانی» زده می‌شود):

1. آدرس ذخیره‌شده در پنل (ستون `updateManifestUrl` در تنظیمات فروشگاه)
2. متغیر محیطی `UPDATE_MANIFEST_URL`
3. **گیت‌هاب رسمی اسکریپت (پیش‌فرض v30):**
   `https://raw.githubusercontent.com/Alextaylorvhjnf/TAJSHOPBYAlex/main/updates/update-manifest.json`
4. مانیفست نمونهٔ همراه اسکریپت: `/update/update-manifest.json` (فقط حالت آفلاین/بی‌ارتباط)

---

## ۲) میزبانی به‌روزرسانی

### ⭐ گیت‌هاب رسمی این فروشگاه (فعال و پیش‌فرض)

از نسخهٔ v30، منبع اصلی به‌روزرسانی ریپوی رسمی خود اسکریپت است:

- **ریپو:** `https://github.com/Alextaylorvhjnf/TAJSHOPBYAlex`
- **مانیفست (پیش‌فرض، بدون هیچ تنظیمی):**
  `https://raw.githubusercontent.com/Alextaylorvhjnf/TAJSHOPBYAlex/main/updates/update-manifest.json`
- **بسته‌های ZIP:** `updates/taj-electronics-update-<نسخه>.zip` در همان ریپو

دکمهٔ «بررسی به‌روزرسانی» پنل، وقتی نه آدرسی در پنل ذخیره شده باشد و نه
`UPDATE_MANIFEST_URL` ست شده باشد، **خودش همین مانیفست گیت‌هاب را چک می‌کند**.

**دو راه نصب:**
1. پنل → «اسکریپت به‌روزرسانی» → بررسی → دانلود و نصب (فقط مدیر ارشد).
   در نصب داکری، فایل‌های `public/` و اسکیمای دیتابیس داخل کانتینر اعمال
   می‌شود؛ برای اعمال کامل `src/` راه ۲ را اجرا کنید.
2. ترمینال سرور: `cd ~/taj-electronics && ./update.sh` — ZIP را از گیت‌هاب
   دانلود، چک‌سام را تأیید، کدها را اعمال و تصویر داکر را rebuild می‌کند.
   `--check` فقط بررسی می‌کند و `--force` نصب مجدد همان نسخه است.

**نصب قدیمی (v29.2 و قبل‌تر) — فقط یک بار:**
```bash
cd ~/taj-electronics
curl -sL -o update.sh https://raw.githubusercontent.com/Alextaylorvhjnf/TAJSHOPBYAlex/main/updates/update.sh
chmod +x update.sh && ./update.sh
```

> انتشار نسخهٔ جدید = push در همان ریپو (فایل `updates/update-manifest.json`
> + ZIP). سازندهٔ بسته از `scripts/release-update.mjs` داخل ریپو استفاده
> می‌کند که همهٔ این‌ها را خودکار می‌سازد و پوش می‌کند.

### روش جایگزین) سرور / هاست خودتان (cPanel و…)

اگر سرور فروشگاه به گیت‌هاب دسترسی نداشت:

1. در هاست، پوشه‌ای مثل `public_html/updates/` بسازید.
2. `update-manifest.json` و فایل ZIP نسخه را داخل آن آپلود کنید.
3. آدرس مانیفست را در پنل ذخیره کنید، مثلاً:
   `https://shoping.alexvshop.ir/updates/update-manifest.json`

**مزایا:** بدون وابستگی به سرویس خارجی؛ در دسترس‌ترین گزینه برای سرورهای ایران.
**معایب:** پهنای باند هاست خودتان مصرف می‌شود؛ HTTPS الزامی است (چک‌سام و مرورگرها روی http مشکلی ندارند اما اعتماد کمتری دارد).

> هر بار نسخهٔ جدید منتشر می‌کنید، فقط محتوای `update-manifest.json` را به نسخهٔ جدید تغییر دهید — آدرس مانیفست که در پنل ذخیره شده ثابت می‌ماند.

---

## ۳) ساخت بستهٔ به‌روزرسانی (ZIP)

بستهٔ به‌روزرسانی **فقط فایل‌های تغییرکرده** را با همان ساختار پوشه‌های پروژه شامل می‌شود:

```
taj-electronics-29.3.0.zip
├── src/…                       ← فایل‌های تغییرکرده/جدید کد
├── public/…                    ← فایل‌های استاتیک جدید (به‌جز public/uploads!)
├── scripts/…                   ← اسکریپت‌های جدید
├── prisma/schema.prisma        ← فقط اگر مدل‌ها تغییر کرده (مهاجرت خودکار اجرا می‌شود)
├── next.config.ts              (اختیاری)
├── tailwind.config.ts          (اختیاری)
├── tsconfig.json               (اختیاری)
├── postcss.config.mjs          (اختیاری)
├── components.json             (اختیاری)
├── eslint.config.mjs           (اختیاری)
└── package.json                (فقط اگر وابستگی جدید اضافه شده)
```

مثال ساخت با ترمینال:

```bash
# از داخل پوشهٔ سورس، فقط مسیرهای مجاز را زیپ کنید
zip -r taj-electronics-29.3.0.zip src public scripts prisma/schema.prisma package.json
sha256sum taj-electronics-29.3.0.zip   # مقدار خروجی را در مانیفست بگذارید
```

**قوانین مهم:**

- حذف فایل پشتیبانی نمی‌شود — بسته فقط فایل اضافه/جایگزین می‌کند. اگر فایلی باید حذف شود، در `notes` ذکر کنید تا دستی حذف شود.
- حداکثر حجم بسته ۲۰۰ مگابایت و حداکثر ۵۰۰۰ فایل است.
- فایل‌های اجرایی داخل بسته هرگز اجرا نمی‌شوند (اسکریپت‌های npm از ZIP اجرا نمی‌شوند).

---

## ۴) ایمنی — اسکریپت به‌روزرسان از چه چیزی محافظت می‌کند؟

- **هرگز دست نمی‌زند** به: `db/` (دیتابیس سفارش‌ها/کاربران/محصولات)، `.env` و `.env.*`، `uploads/` و `public/uploads/`، `node_modules/`.
- **فقط** این مسیرها را می‌تواند جایگزین کند: `src/`، `public/` (به‌جز uploads)، `scripts/`، `prisma/` و فایل‌های پیکربندی ریشه.
- هر مسیر **مطلق**، شامل `..` یا **symlink** → کل بسته رد می‌شود.
- اگر `sha256` در مانیفست باشد و با فایل دانلودی مطابقت نداشته باشد → **قبل از هر تغییری** لغو می‌شود.
- قبل از جای‌گذاری، از فایل‌های فعلی در `db/update-backup-<timestamp>/` پشتیبان گرفته می‌شود (۲ پشتیبان آخر نگه داشته می‌شود).
- مهاجرت دیتابیس فقط با `prisma db push` (additive) انجام می‌شود؛ داده‌ها دست نمی‌خورند.
- دانلود ZIP روی سرور انجام می‌شود (نه مرورگر شما) — روی سرور ایران یا GitHub بسته به آدرسی که انتخاب کرده‌اید.

---

## ۵) بعد از نصب

1. بعد از اتمام مراحل، فرآیند فروشگاه **خودکار ری‌استارت** می‌شود (سیاست restart کانتینر Docker / pm2 / systemd) و کد جدید بالا می‌آید. حدود ۳۰ ثانیه سایت در دسترس نیست.
2. در پنل، پیام «راه‌اندازی مجدد در حال انجام است — صفحه را رفرش کنید» را می‌بینید؛ بعد از بالا آمدن، صفحه را رفرش کنید.
3. اگر `package.json` در بسته بود (وابستگی جدید)، **یک‌بار** این دستور را اجرا کنید:
   ```bash
   docker compose exec app bun install
   ```
   (در هاست بدون Docker: `bun install` در پوشهٔ پروژه.)
4. مهاجرت دیتابیس (در صورت تغییر `prisma/schema.prisma`) خودکار اجرا شده است؛ در صورت خطا در لاگ ثبت می‌شود و اپ خودش با self-heal ستون‌های لازم را اضافه می‌کند.

> در محیط توسعه (`next dev`) ری‌استارت خودکار انجام نمی‌شود — هات‌ریلود خودش کد جدید را فعال می‌کند و فقط کافی است صفحه رفرش شود.

---

## ۶) بازگشت (Rollback)

اگر بعد از به‌روزرسانی مشکلی پیش آمد:

```bash
# آخرین پشتیبان را ببینید
ls db/ | grep update-backup
# فایل‌های پشتیبان را روی پروژه برگردانید (مثال):
cp -r db/update-backup-2026-09-08T10-30-00-000Z/src ./src
cp    db/update-backup-2026-09-08T10-30-00-000Z/package.json ./package.json
# سپس ری‌استارت:
docker compose restart app
```

پشتیبان فقط فایل‌هایی را نگه می‌دارد که جایگزین شده‌اند — دیتابیس همیشه دست‌نخورده است، پس داده‌ها نیازی به بازگردانی ندارند.

---

## ۷) پرسش‌های پرتکرار

**آیا دیتابیس دست می‌خورد؟**
خیر. فایل‌های `db/` هرگز بازنویسی یا حذف نمی‌شوند؛ فقط در صورت تغییر `prisma/schema.prisma`، ستون‌های جدید به‌صورت additiv اضافه می‌شوند — سفارش‌ها، کاربران و محصولات ۱۰۰٪ سالم می‌مانند.

**آیا رمزها و تنظیمات .env حفظ می‌شوند؟**
بله. `.env` و `.env.*` هرگز جزو مسیرهای مجاز نیستند.

**عکس‌های بارگذاری‌شدهٔ کاربران (uploads) چه می‌شوند؟**
دست نمی‌خورند. مسیر `uploads/` و `public/uploads/` صریحاً در بسته ممنوع است و اگر باشد کل بسته رد می‌شود.

**اگر اینترنت سرور قطع باشد؟**
مرحلهٔ دانلود با پیام فارسی خطا متوقف می‌شود و هیچ تغییری اعمال نمی‌شود.

**اگر دو نفر همزمان به‌روزرسانی را اجرا کنند؟**
فقط یک فرآیند هم‌زمان مجاز است؛ درخواست دوم پیام «یک به‌روزرسانی در حال اجراست» می‌گیرد.

**چه کسی می‌تواند به‌روزرسانی اجرا کند؟**
بررسی نسخه: هر نقش مدیریت. ذخیرهٔ آدرس مانیفست: مدیر ارشد/ادمین. اجرای نصب: فقط **SUPER_ADMIN**.

**مانیفست نمونهٔ همراه اسکریپت چیست؟**
`/update/update-manifest.json` — نسخهٔ فعلی خودتان را اعلام می‌کند تا حالت «به‌روز هستید» را ببینید. با ذخیرهٔ آدرس مانیفست واقعی، همان لحظه فعال می‌شود.
