# یادداشت انتشار v34.1 — تاج الکترونیکس (نسخهٔ داخلی ۲۹.۰.۱)

این نسخه فقط یک هدف دارد: **رفع کامل سه خطای واقعی که روی سرور شما گزارش شده بود** — بدون تغییر در هیچ قابلیتی از فروشگاه.

---

## ۱) 🩺 خطای ویزارد و دیتابیس هنگام نصب (روت‌کاو واقعی)

### علت
بستهٔ v34 یک فایل `node_modules/.bin/prisma` «باندل‌شدهٔ» تک‌فایلی داشت که فایل‌های WASM موتور خود را **کنار خودش در پوشهٔ `.bin/`** جستجو می‌کرد، در حالی که آن‌ها در `prisma/build/` قرار دارند. نتیجه:

```
Error: ENOENT: no such file or directory, open '…/node_modules/.bin/prisma_schema_build_bg.wasm'
```

یعنی گام «راه‌اندازی دیتابیس» در ویزارد `/install` همیشه شکست می‌خورد («ایجاد ساختار دیتابیس ناموفق بود») و چون ادامهٔ ویزارد به همین گام گره خورده بود، ساخت ادمین و تکمیل نصب هم متوقف می‌شد.

### راه‌حل (دو لایه)
1. **شیم اصلاح‌شده** — `.bin/prisma` حالا یک لانچر سبک است که CLI واقعی (`prisma/build/index.js`) را اجرا می‌کند تا WASM‌ها از مسیر درست خود پیدا شوند.
2. **اولویت مسیر در ویزارد** — `db-setup` حالا مستقیماً `node prisma/build/index.js` را ترجیح می‌دهد (`.bin` فقط پشتیبان است)؛ حتی اگر روزی `.bin` دوباره خراب شود، ویزارد کار می‌کند.

## ۲) 🤖 خطای ۴۰۳ در «تست هوش مصنوعی»

### علت
مسیر API تست (`POST /api/ai/test`) اصلاً در بستهٔ v34 **وجود نداشت** — دکمهٔ «تست اتصال» در تنظیمات ← هوش مصنوعی به یک مسیر غایب می‌زد و به‌جای پیام روشن، خطای خام می‌گرفت. (و اگر موتور داخلی روی سرور شما اجازهٔ دسترسی نداشته باشد، همان خطای ۴۰۳ هم ممکن است.)

### راه‌حل
- مسیر `/api/ai/test` **اضافه شد** (فقط برای مدیر، با ثبت در گزارش فعالیت).
- تشخیص خطاها حالا فارسی و دقیق است: ۴۰۳/۴۰۱ → «موتور داخلی فقط در محیط دموی ساخت همین نسخه فعال است؛ GapGPT را با کلید API خودتان انتخاب کنید»، ۴۲۹ → «موتور شلوغ است، کمی بعد دوباره تست بگیرید»، تایم‌اوت و خطای شبکه هم پیام خودشان را دارند.
- تستِ موتور داخلی حالا سقف ۱۵ ثانیه دارد (دیگر بی‌پایان نمی‌چرخد).
- تا وقتی AI واقعی (GapGPT) تنظیم نشده باشد، ویجت چت فروشگاه با موتور قطعی داخلی (جستجوی محصول/قیمت/موجودی/پیگیری سفارش) به مشتری پاسخ می‌دهد — هیچ‌وقت ویجت خالی نمی‌ماند.

## ۳) 📦 دیتابیس دموی ناقص + خطای «duplicate column» هنگام بوت

### علت
`db-seed/catalog.db` بستهٔ v34 سه جدولِ نسخه (`TemplateContent`، `TelegramBotSettings`، `TelegramSubscriber`) را نداشت → ربات تلگرام هنگام بوت خطای «جدول وجود ندارد» می‌داد. ضمناً Prisma Client در حالت production برای هر باندل جدا ساخته می‌شد و «خوددرمانی ستون‌ها» دو بار موازی اجرا می‌شد → خطای `duplicate column name: storeChrome` در لاگ.

### راه‌حل
- دیتابیس دمو با اسکیمای کامل ۳۷ جدولی **بازساخته شد** (۱۰۰ محصول، بدون کاربر — ویزارد مثل قبل فعال می‌ماند).
- Prisma Client حالا **یک نمونهٔ سراسری، تنبل و قابل‌ریست** است: پس از `prisma db push` (که فایل SQLite را زیر پای اتصالات باز بازمی‌نویسد) کلاینت به‌صورت خودکار بازسازی می‌شود — دیگر کرش‌های مقطعی بعد از گام دیتابیس رخ نمی‌دهد.
- خوددرمانی ستون‌ها: پرچم جهانی + تحمل خطای duplicate column (اجرا فقط یک بار، بدون هشدار کاذب).

## ۴) 🛡️ پایداری بوت (پورت‌شده از v33)
- `.env` بدون `DATABASE_URL` (میراث Docker) حالا **خوددرمانی** می‌شود: ویزارد مسیر استاندارد `file:<app>/db/custom.db` را در `.env` می‌نویسد — بدون ریسپانسیو ۵۰۰.
- صفحهٔ اصلی و کل بخش فروشگاه حالا **مرز خطای فارسی** دارند («خطایی پیش آمد… تلاش دوباره / رفتن به نصب») به‌جای صفحهٔ انگلیسی Next.js.

## ۵) 🛒 دکمهٔ «خرید از ربات تلگرامی» در فوتر همهٔ قالب‌ها
- در **هر ۲۶ قالب** (همهٔ واریانت‌های فوتر کروم + فوتر مشترک پیش‌فرض) یک دکمهٔ کپسولی با آیکن تلگرام اضافه شد.
- آدرس دکمه از **تنظیمات ← فروشگاه ← «لینک ربات خرید تلگرامی»** می‌آید؛ `@username` یا لینک `t.me/…` بپذیرید.
- اگر خالی بگذارید، سیستم **خودکار** از نام کاربریِ رباتِ تنظیم‌شده در «تنظیمات ← ربات تلگرامی» استفاده می‌کند (ربات فعال = دکمه ظاهر می‌شود).
- خالی بودن هر دو = دکمه مخفی (هیچ چیزی نمی‌شکند).

---

## نصب — مثل همیشه یک خط
فایل ZIP را در روت سرور آپلود کنید و:
```bash
sudo bash -c 'command -v unzip >/dev/null || (apt-get update -qq && apt-get install -y -qq unzip) 2>/dev/null || dnf install -y -q unzip; unzip -oq "<نام-فایل>.zip" && bash install.sh'
```
سپس `http://دامنه-یا-IP/install` را باز کنید و مدیر کل بسازید.

## ارتقا از v34؟
دقیقاً همین دستور نصب را روی نصب موجود اجرا کنید — کد جایگزین می‌شود و **دیتابیس، آپلودها و .env شما دست‌نخورده می‌مانند** (نصب مجدد = ارتقا).
